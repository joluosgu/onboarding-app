name: flutter_onboarding_app
version: 1.0.0+1
description: App de onboarding con Supabase

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  flutter_riverpod: ^2.4.0
  http: ^1.0.0
  intl: ^0.18.0
  flutter_hooks: ^0.20.0
  cupertino_icons: ^1.0.2
  url_launcher: ^6.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/galatea_logo_gris.png
